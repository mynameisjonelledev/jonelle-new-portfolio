import { HoverState } from '../types';
declare const useRegisterHover: () => (hoverState: HoverState) => {
    onMouseEnter: (e: any) => void;
    onMouseLeave: (e: any) => void;
};
export { useRegisterHover };
